apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - hpa-scale-to-zero.yaml
  - spot-instance-tolerations.yaml

commonLabels:
  app.kubernetes.io/part-of: ecommerce-microservices
  app.kubernetes.io/component: finops

